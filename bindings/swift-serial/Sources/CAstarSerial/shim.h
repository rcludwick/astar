/* Umbrella header for the CAstarSerial systemLibrary target: pulls in the
 * committed astarserial.h by relative path (single source of truth). */
#include "../../../../crates/astar-serial-sys/include/astarserial.h"
